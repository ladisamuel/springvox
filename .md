https://threejsdemos.com/demos/basics/ambient-directional

for ambient

const ambient = new THREE.AmbientLight(0xffffff, 0.3)
scene.add(ambient)
const dir = new THREE.DirectionalLight(0xffffff, 1)
dir.position.set(2, 2, 2)
scene.add(dir)